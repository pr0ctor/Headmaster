@model IEnumerable<Headmaster.Models.CourseGrades>

@{
    ViewBag.Title = "Index | Course Grades";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Grades.Grade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Registrations.RegistrationID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Students.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Grades.Grade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Registrations.RegistrationID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Students.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CourseGradeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CourseGradeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CourseGradeID })
        </td>
    </tr>
}

</table>
