@model IEnumerable<Headmaster.Models.Minors>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MinorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentMinors.StudentMinorID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MinorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentMinors.StudentMinorID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MinorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MinorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MinorID })
        </td>
    </tr>
}

</table>
