@model IEnumerable<Headmaster.Models.Prerequisites>

@{
    ViewBag.Title = "Index | Prequisites";
}

<h2>Index | Prequisites</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Course
        </th>
        <th>
            Prerequisites
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Courses.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Courses1.CourseName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrerequisiteID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrerequisiteID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrerequisiteID })
        </td>
    </tr>
}

</table>
